p Welcome to our Dagbok
hr
-@posts.each do |post|
  a href="post/#{post.id}"
    #post
      h3 style="float: left;" = post.title
      h6 style="padding-top: 20px;" = post.created_at.strftime('%F')
      br
  hr
-if session[:user]
  form action="/search" method="post"
    input.form-control type="text" name="search" placeholder="Search"
    button class="btn btn-info"  type="submit" Search
  form action="/create" method="post"
    input.form-control type="text" name="title" placeholder="title"
    textarea.form-control name="text" rows="6" cols="50" placeholder="kära dagbok..."
    button class="btn btn-success"  type="submit" Spara
